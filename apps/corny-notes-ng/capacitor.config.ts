import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'corny-notes-ng',
  webDir: '../../dist/apps/corny-notes-ng',
  bundledWebRuntime: false,
};

export default config;
